import requests

from error_handler.http_exception import HTTPException


def send_message(message :str):
    # payload = {
    #     "message": f"{message}"
    # }
    # response = requests.request("POST", SLACK_WEBHOOK, json=payload)
    # return HTTPException.check_error(response)
    pass